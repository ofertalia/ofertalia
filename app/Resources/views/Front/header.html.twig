<nav class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">Ofertalia</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <form class="navbar-form navbar-right" action="{{ path('fos_user_security_check') }}" method="post">
                {% if isUserLogged %}
                    <small style="color: #f6f6f6">Bienvenido {{ userName }}!</small>
                    <a href="{{ path("fos_user_security_logout") }}" class="btn btn-danger">Log out!</a>
                {% else %}
                    <input type="hidden" name="_csrf_token" value="{{ csrfToken }}" />
                    <div class="form-group">
                        <input type="text" placeholder="Nombre" class="form-control" id="username" name="_username" value="" required="required">
                    </div>
                    <div class="form-group">
                        <input type="password" placeholder="Password" class="form-control" id="password" name="_password" required="required">
                    </div>
                    <button type="submit" class="btn btn-primary" id="_submit" name="_submit" value="Login">Sign in</button>
                    <button type="submit" class="btn btn-success">Get started</button>
                {% endif %}
            </form>
        </div>
    </div>
</nav>